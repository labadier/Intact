[project]
name = "intact"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dvc>=3.67.1",
    "dvc-ssh>=4.3.0",
    "sentence-transformers>=5.5.1",
]

[dependency-groups]
dev = [
    "dvc-ssh>=4.3.0",
]
